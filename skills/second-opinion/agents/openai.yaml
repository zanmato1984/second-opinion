interface:
  display_name: "Second Opinion"
  short_description: "Reviewer-centric prompt compiler"
  default_prompt: "Run the prompt-compiler review workflow on a diff and produce review.md and review.json."
